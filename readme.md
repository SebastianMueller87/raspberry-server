# Raspberry-Server Testproject

# Requirements

# On Raspberrypi:

```
git clone https://github.com/SebastianMueller87/quick2wire-gpio-admin.git
cd quick2wire-gpio-admin
make
sudo make install
sudo adduser $USER gpio
```

* Modify

* npm install
* gulp
* start server
